version: '3.8'

services:
  app:
    image: mcr.microsoft.com/devcontainers/dotnet:1-10.0
    volumes:
      - ".:/workspace:cached"
    command: sleep infinity
    networks:
      - app-network

networks:
  app-network:
    driver: bridge